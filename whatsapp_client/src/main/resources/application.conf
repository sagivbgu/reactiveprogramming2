akka {
    loglevel = "OFF"
    log-dead-letters = 10

  actor {
    provider = remote
    warn-about-java-serializer-usage = false
  }

  remote {
    managing-server = "akka://whatsapp_manager@192.168.1.16:2552/user/manager"
    artery {
      enabled = on
      transport = tcp
      canonical.port = 0
  }
 }
}