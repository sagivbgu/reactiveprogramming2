akka {
    loglevel = "OFF"
    log-dead-letters = 10

  actor {
    provider = remote
    warn-about-java-serializer-usage = false
  }

  remote {
    artery {
      enabled = on
      transport = tcp
      canonical.port = 0
  }
 }
}